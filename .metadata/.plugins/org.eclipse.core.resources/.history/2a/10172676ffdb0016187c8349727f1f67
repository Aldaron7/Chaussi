package chaussia.shared.test;

import chaussia.shared.gebaeude.Gebaeude;
import chaussia.shared.gebaeude.Gebaeude.Typ;
import chaussia.shared.gebaeude.produktion.AbstractProduktionsgebaeude;
import chaussia.shared.gebaeude.produktion.ProduktionsgebaeudeFactory;

public class Test
{

    public static void main(String[] args)
    {
        Test.testGebaude();
    }

    public static void testGebaude()
    {
        Gebaeude gebaeude = new ProduktionsgebaeudeFactory().create(Typ.HOLZFAELLER);
        System.out.println(gebaeude.getClass().getSimpleName());
        ((AbstractProduktionsgebaeude) gebaeude).setArbeitet(true);
        ((AbstractProduktionsgebaeude) gebaeude).setBesetzt(true);
        for (int i = 0; i < 5; i++)
        {
            System.out.println("bauzeit:" + gebaeude.getBauzeit());
            System.out.println(((AbstractProduktionsgebaeude) gebaeude).getErtrag());
            gebaeude.reduziereBauzeit();
        }
        gebaeude.upgrade();
        for (int i = 0; i < 5; i++)
        {
            System.out.println("upgradedauer:" + gebaeude.getUpgradedauer());
            System.out.println(((AbstractProduktionsgebaeude) gebaeude).getErtrag());
            gebaeude.reduziereUpgradeDauer();
        }
    }

}
